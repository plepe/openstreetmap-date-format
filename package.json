{
  "dependencies": {
    "moment": "^2.23.0"
  },
  "name": "openstreetmap-date-format",
  "version": "0.0.0",
  "description": "Format openstreetmap dates (like start_date) in a localized string",
  "main": "src/index.js",
  "repository": "https://github.com/plepe/openstreetmap-date-format",
  "author": "Stephan Bösch-Plepelits <skunk@xover.mud.at>",
  "license": "GPL-3.0",
  "devDependencies": {
    "browserify": "^16.2.3",
    "mocha": "^5.2.0",
    "standard": "^12.0.1"
  },
  "scripts": {
    "build-test": "browserify --debug test/*.js -o dist/test.js",
    "lint": "standard --fix index.js src/*.js test/*.js",
    "test": "mocha --bail"
  }
}
